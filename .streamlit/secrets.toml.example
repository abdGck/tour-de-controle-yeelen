# Fichier exemple — renomme en secrets.toml pour usage local
# Sur Streamlit Cloud, ces valeurs se mettent dans App Settings > Secrets

# URL de ton serveur Flask (Render)
FLASK_URL = "https://ton-app.onrender.com"
